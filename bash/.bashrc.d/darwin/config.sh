#!/bin/bash

# Platform specific paths
AUTOENV="$(brew --prefix autoenv)/activate.sh"
BASH_COMPLETION="$(brew --prefix)/etc/bash_completion"
LESSPIPE="$(brew --prefix lesspipe)/bin/lesspipe.sh"
NVM="$(brew --prefix nvm)/nvm.sh"
